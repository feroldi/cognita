name: cognita
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  bloc: ^0.9.3
  flutter_bloc: ^0.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.5.3

flutter:
  uses-material-design: true
